# Mini Store - CSS Global

This project implements the styling for a game store application using **Global CSS**.

## Project Structure
- **Styles**: Global CSS files (`layout.css`, `card.css`, `home.css`)

